import React from 'react';
import './Block.scss'

const Block = () => {
    return (
        <div className={'wrapper'}>
            <div className="block">
                Адаптив?
            </div>
        </div>
    );
};

export default Block;
